# Media Control — ARCHIVED

> **Archived:** This spec has been replaced by `media-player`.
> 
> `media-player` covers the same use cases (play/pause/skip/volume) plus intelligent
> song search across YouTube, local files, and Spotify with agent-driven fallback.
> 
> See `openspec/specs/media-player/spec.md` for the current specification.
